[tool.poetry]
name = "l9format"
version = "0.1.0"
description = "l9format"
authors = ["Danny Willems <danny@badaas.be>"]

[tool.poetry.dependencies]
python = "^3.7"
serde = "^0.8.1"

[tool.poetry.dev-dependencies]
pytest = "*"
black = "^21.5b1"
mypy = "*"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"